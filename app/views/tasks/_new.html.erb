<%= form_for(@task , :url => '/tasks/create' , :remote => true ) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>

  <div class="field">
     Affectée à s<br>
    <% @option = options_from_collection_for_select(@users, :id, :name) %>
    <%= f.select "users", @option , :multiple => true %>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
