
  <div class="col-md-12">
  <div class="col-md-6">
   <h1 class="page-header">Administrateurs</h1>
  <div class="panel panel-default">
    <div class="panel-heading">
    <table class="table">
      <thead style="text-align:center">
        <th>Nom</th>
    <th> 
     <form class="navbar-form" role="search" data-remote="true" action="/list">
    <div class="input-group">
        <input type="text" class="form-control" placeholder="Search" value="<%= params[:search_admin] %>" name="search_admin">
        <div class="input-group-btn">
            <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
        </div>
        
    </div>
  </form>
  </th>
      </thead>
      <tbody>
      <% @admins.each do |user| %>
        <tr>
        <td><%= user.name %></td>
         <td>
          <a href="/show/<%=user.id%>" class="btn btn-info btn-lg" data-remote=true><span class="glyphicon glyphicon-eye-open"></span></a>
          <a href="/edit/<%=user.id%>" class="btn btn-primary btn-lg" data-remote=true id="edit"><span class="glyphicon glyphicon-edit"></span></a>
          <a href="/list?destroy_user=<%=user.id%>" class="btn btn-danger btn-lg" data-confirm ="Etes vous sur(e) ?" data-remote=true >
            <span class="glyphicon glyphicon-remove"></span> 
          </a>
        </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  </div>
  </div>

  <div class="col-md-6">
  <h1 class="page-header">Revendeurs</h1>
  <div class="panel panel-default">
    <div class="panel-heading">
    <table class="table">
      <thead>
        <th>Nom</th>
        <th> 
          <form class="navbar-form" role="search" data-remote="true" action="/list">
            <div class="input-group col-md-6">
                <div class="input-group-btn col-md-6">
                  <input type="text" class="form-control" placeholder="Search" value="<%= params[:search_reseller]%>" name="search_reseller">
                </div>
                <div class="input-group-btn col-md-6">
                    <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                </div>  
            </div>
            <div class="input-group col-md-6">
            <a href="#" class="btn btn-primary btn-lg" data-remote=true id="new" data-toggle="modal" data-target="#myModal2">
                <span class="glyphicon glyphicon-plus"></span> Nouvel Utilisateur
              </a>
              <!--a href="/new" class="btn btn-primary btn-lg" data-remote=true id="new"><span class="glyphicon glyphicon-plus"></span></a>
                -->
            </div>
          </form>
        </th>
      </thead>
      <tbody>
      <% @resellers.each do |user| %>
        <tr>
        <td>
         <%= user.name %>
        </td>
        <td>
          <a href="/show/<%=user.id%>" class="btn btn-info btn-lg" data-remote=true><span class="glyphicon glyphicon-eye-open"></span></a>
          <a href="/edit/<%=user.id%>" class="btn btn-primary btn-lg" data-remote=true id="edit"><span class="glyphicon glyphicon-edit"></span></a>
          <a href="/list?destroy_user=<%=user.id%>" class="btn btn-danger btn-lg" data-confirm ="Etes vous sur(e) ?" data-remote=true >
            <span class="glyphicon glyphicon-remove"></span> 
          </a>
        </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  </div>
  </div>
  </div>
